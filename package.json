{
  "name": "api",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test:ci": "yarn lerna run test --since"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com/"
  },
  "devDependencies": {
    "husky": "^4.2.3",
    "lerna": "^3.20.0"
  }
}
